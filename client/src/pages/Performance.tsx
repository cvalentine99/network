import { PageHeader } from "@/components/DashboardWidgets";

export default function Detections() {
  return (
    <div>
      <PageHeader
        title="Detections"
        subtitle="Security and performance detections"
      />
    </div>
  );
}
